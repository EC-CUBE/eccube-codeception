server {
    listen      8881;
    server_name eccube.case-k.com;
    location / {
        proxy_cache off;
        proxy_set_header host $host:$server_port;
        proxy_pass http://localhost:8081/;
        client_max_body_size 20M;
    }
}
